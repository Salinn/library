<% if object.errors.any? %>
  <div id="error_explanation has-error">
    <h2><%= pluralize(object.errors.count, "error") %> prohibited this user from being saved:</h2>
    <ul id='error-list'>
      <% object.errors.full_messages.each do |msg| %>
        <li> * <%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>